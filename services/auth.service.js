/**
 * Service in here
 */
const express = require('express');

const authService = {
    login: async (req, res) => {
        try {
            if (req.body) {
                if (req.body.userName && req.body.passWord) {
                    
                } else {
                    
                }
            }
        } catch (error) {
            
        }
    }
}